subscription MessageSubscription($interlocutorId: ID!) {
  message(interlocutorId: $interlocutorId) {
    id
    text
    date
    isRead
    text
    sender {
      id
      image
      firstName
      lastName
    }
  }
}
