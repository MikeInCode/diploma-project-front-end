import { makeStyles } from '@material-ui/core'
import { Color } from '../../theme'

export const useLinearProgressClasses = makeStyles({
  root: {
    background: Color.Green2
  },
  bar: {
    background: Color.Green
  }
})
