import { makeStyles } from '@material-ui/core'

export const useAssessmentDrawerStyles = makeStyles(
  {
    listItemText: {
      display: 'flex',
      flexDirection: 'row-reverse',
      alignItems: 'flex-end'
    }
  },
  { name: 'AssessmentDrawerStyles' }
)
