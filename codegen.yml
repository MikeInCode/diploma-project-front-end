schema: http://localhost:3001/graphql
documents: src/api/**/*.ts
generates:
  src/graphQLTypes.ts:
    config:
      preResolveTypes: true
      skipTypename: true
    plugins:
      - typescript
      - typescript-operations
